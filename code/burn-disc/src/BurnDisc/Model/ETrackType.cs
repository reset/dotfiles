namespace BurnDisc.Model;

//
// The two kinds of track that appear on a mixed-mode game CD: a single data
// track (the game) followed by optional CD-audio tracks (the soundtrack).
//
internal enum ETrackType {
    Data,
    Audio
}
