#!/usr/bin/env bash
# Upgrade the system Nix installation to the latest stable release.
#
# Requires sudo because Nix is installed multi-user on macOS — the daemon
# and store are owned by root. Without sudo, nix upgrade-nix fails silently
# or errors with a permissions message.
set -euo pipefail

sudo nix upgrade-nix "$@"
