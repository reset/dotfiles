#!/usr/bin/env bash
set -euo pipefail
bin="/Users/reset/code/burn-disc/dist/burn-disc"
[[ -x "$bin" ]] || { echo >&2 "burn-disc not built — run: make -C ~/code/burn-disc publish"; exit 1; }
exec "$bin" "$@"
